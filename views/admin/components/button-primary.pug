mixin button-primary(text, extraClass, icon, data)

  button.btn.btn-primary(class=extraClass type='button' data-cms=data)
    if icon
      i.me-2.bi(class=`bi-${icon}`)
    span= text
    span.ms-2.spinner-border.spinner-border-sm.d-none